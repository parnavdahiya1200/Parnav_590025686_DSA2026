#include <stdio.h.>

int main()
{
    int x,a=0,b=0;
    printf("ENTER THE NUMBER \n");
    scanf("%d", &x);

    while(x>0)
    {
        a=x%10;
        b=b*10+a;
        x=x/10;
    }

    printf("REVERSED NUMBER : %d", b);
    return 0;
}
